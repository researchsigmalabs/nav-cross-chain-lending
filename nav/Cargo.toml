[package]
name = "nav"
version = "0.1.0"
edition = "2021"
description = "At-par cross-chain NAV computation for the σ-Labs / Ellen Capital Cross-chain USDC Lending vault (sigmaUSDC) — the exact valuation math the keeper submits on-chain."
license = "Apache-2.0"
repository = "https://github.com/researchsigmalabs/cross-chain-usdc-lending"
readme = "README.md"

[dependencies]
alloy-primitives = "1.0"
eyre = "0.6"
